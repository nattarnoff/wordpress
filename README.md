# WordPress theme repository

This repository contains a custom theme at:

- `/home/runner/work/wordpress/wordpress/wp-content/themes/single-column`

## Theme structure update

The `single-column` theme now includes the required block-theme template file:

- `style.css` (already present)
- `templates/index.html` (added)

Additional theme documentation has been added in:

- `wp-content/themes/single-column/README.txt`
